# Next Steps for this Gallery

1. **Re-upload original 4 photos** into `characters/green-eyed-woman/refs/` (ask Grok to help once you download them)
2. Enable Git LFS: `git lfs track "*.jpg" "*.png"` then push the actual images
3. Turn on GitHub Pages (Settings → Pages → main branch /docs or root) for a public/private gallery
4. Ask Grok: “animate the latest kneeling pose” or “generate 3 more outfit variations and add them to the repo”
5. Create Issues for ideas: better freckles, different lighting, video, etc.

6. **Self-host gallery images** in `docs/images/` (WebP + JPEG fallback) and update `src`/`srcset` in `docs/index.html` to drop the Drive dependency entirely for max performance

